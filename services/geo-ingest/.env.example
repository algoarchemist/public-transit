OVERPASS_URL=https://overpass-api.de/api/interpreter
OSRM_URL=http://localhost:5000 # self-hosted, see infra/docker/osrm/build.sh; https://router.project-osrm.org as a fallback
DATABASE_URL=postgres://setutrack:setutrack@localhost:5432/setutrack

# Optional. Unlocks denser real bus-stop discovery (Places Nearby Search) and
# traffic-conditioned segment durations (Distance Matrix) in app/google_maps.py.
# Needs a Google Cloud project with billing enabled and Places API + Distance
# Matrix API turned on. Leave unset to keep using the free OSRM/OSM path.
GOOGLE_MAPS_API_KEY=
