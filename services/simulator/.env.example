MQTT_BROKER_HOST=localhost
MQTT_BROKER_PORT=1883
SNAPSHOT_LABEL=mohali-tricity
# Optional — enables a best-effort TimescaleDB write in backfill mode. Backfill
# stays CSV-only (data/backfill/<label>/) if this is unset or unreachable.
DATABASE_URL=postgres://setutrack:setutrack@localhost:5432/setutrack
